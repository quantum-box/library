# Library API the guide documents. Every example and every generated list
# comes from this deployment.
VITE_LIBRARY_API_BASE_URL=https://library-api.txcloud.app

# Library Client the reader issues keys in.
VITE_LIBRARY_CLIENT_URL=https://planetlibrary.txcloud.app
