{
  "name": "library",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "npm run --workspace apps/web dev",
    "client:dev": "npm --prefix apps/client run dev",
    "client:build": "npm --prefix apps/client run build",
    "client:test": "npm --prefix apps/client run test",
    "client:type-check": "npm --prefix apps/client run type-check",
    "build": "npm run --workspace apps/web build",
    "lint": "npm run --workspace apps/web lint",
    "test": "npm run --workspace apps/web test",
    "tsc": "npm run --workspace apps/web ts",
    "smoke:library-rest-e2e": "scripts/library-rest-e2e-smoke.sh",
    "check:rust-workspace-cycles": "node scripts/check-rust-workspace-cycles.mjs"
  },
  "resolutions": {
    "webpack": "5.101.2",
    "tailwindcss": "3.4.17",
    "vite": "^6.3.5"
  },
  "devDependencies": {
    "vitest": "^3.1.1"
  }
}
