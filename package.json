{
  "name": "library",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "npm run --workspace apps/web dev",
    "build": "npm run --workspace apps/web build",
    "lint": "npm run --workspace apps/web lint",
    "test": "npm run --workspace apps/web test",
    "tsc": "npm run --workspace apps/web ts",
    "check:rust-workspace-cycles": "node scripts/check-rust-workspace-cycles.mjs"
  },
  "resolutions": {
    "webpack": "5.101.2",
    "tailwindcss": "3.4.17",
    "vite": "^6.3.5"
  },
  "devDependencies": {
    "vitest": "^3.1.1"
  }
}
